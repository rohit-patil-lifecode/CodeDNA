---
description: Cost vs payload — is this earning its context?
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `measure` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `measure` section. Run `scripts/measure.py` and read 'Reaching Claude' before anything else.

Arguments: $ARGUMENTS
