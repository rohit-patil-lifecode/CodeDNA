---
description: Record a decision the code cannot explain
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `adr` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `adr` section. Ask for what you cannot know; do not invent alternatives to look thorough.

Arguments: $ARGUMENTS
