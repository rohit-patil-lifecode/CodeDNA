---
description: End-of-session capture: write down what you just learned
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `session` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `session` section. Ask what the code left unexplained while it is still fresh.

Arguments: $ARGUMENTS
