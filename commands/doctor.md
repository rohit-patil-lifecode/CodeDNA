---
description: Health dashboard for the module docs
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `doctor` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `doctor` section.

Arguments: $ARGUMENTS
