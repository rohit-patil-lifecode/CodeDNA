---
description: Trace how something travels the system, end to end
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `flow` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `flow` section. Trace it in the code, not from the module docs, and give every step a real file and symbol.

Arguments: $ARGUMENTS
