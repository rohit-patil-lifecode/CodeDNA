---
description: Detect modules and scaffold their CLAUDE.md + architecture.md
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `setup` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `setup` section and follow it exactly. Show the module list and ask once before writing anything, and give each module its own subagent.

Arguments: $ARGUMENTS
