---
description: Drift report: code changed without its docs (warns by default)
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `check` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `check` section. Prefer running `scripts/check_drift.py`.

Arguments: $ARGUMENTS
