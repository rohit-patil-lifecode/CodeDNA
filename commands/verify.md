---
description: Find doc claims that are no longer true
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `verify` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `verify` section. Run `scripts/verify_claims.py` first, then read for what it cannot check.

Arguments: $ARGUMENTS
