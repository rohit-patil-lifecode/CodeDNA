---
description: Architecture review: coupling, hubs, seams, and traps the code shows but the docs don't
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `audit` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `audit` section and follow it. Run coupling.py, gaps.py and verify_claims.py first — every finding needs evidence before you form an opinion.

Arguments: $ARGUMENTS
