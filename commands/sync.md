---
description: Re-check what the docs claim against the code
---

Read `${CLAUDE_PLUGIN_ROOT}/references/commands.md` and follow its `sync` section.
The safety rules in `${CLAUDE_PLUGIN_ROOT}/SKILL.md` apply.

Read the `sync` section. Past two or three modules, use one subagent per module.

Arguments: $ARGUMENTS
